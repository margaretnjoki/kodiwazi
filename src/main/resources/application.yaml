spring:
  application:
    name: kodiwazi

  datasource:
    url: jdbc:postgresql://localhost:5432/kodiwazi
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true